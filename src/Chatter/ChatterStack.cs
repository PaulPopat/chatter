using Amazon.CDK;
using Constructs;

namespace Chatter
{
    public class ChatterStack : Stack
    {
        internal ChatterStack(Construct scope, string id, IStackProps props = null) : base(scope, id, props)
        {
            // The code that defines your stack goes here
        }
    }
}
